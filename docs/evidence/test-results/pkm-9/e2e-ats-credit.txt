### AC5 — the credit line renders (browser, full local stack)

    pnpm test:e2e
      19 passed (18.8s)

      ✓ 10 [simulated] › e2e/pickem-journey.sim.spec.ts:322:3
           Pick'em merge-gate journey (mixed-week scenario)
           › an ATS league names the book behind the spreads it shows (527ms)

Non-vacuity check — reverted SimulatedProvider to `spreadSource: null`,
re-ran the single test, restored:

    pnpm test:e2e -g 'names the book behind the spreads'
      1 failed
        [simulated] › an ATS league names the book behind the spreads it shows
      8 passed (24.5s)
      exit=1

So the assertion proves the whole chain rather than passing incidentally:
sim fixture -> SimulatedProvider -> sync-odds job -> games.spread_source
-> slate DTO -> the rendered credit.
