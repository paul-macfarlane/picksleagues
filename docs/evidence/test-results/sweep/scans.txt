### QLTY-5 sweep completeness scans
commit: cd650726653638821aa1eebba3d7fc48aa52a786
base:   c29e12a (staging)

==============================================================
1. Remaining // comment blocks directly above an exported declaration
   (non-test source under apps/*/src and packages/*/src)
==============================================================
apps/api/src/lib/default-hook.ts:11-14  -> 15: export const zodValidationHook: Hook<any, any, any, any> = (result, c) => {

   1 residual, justified: default-hook.ts already carries a /** */ contract
   for the export. The remaining // note explains the `any` and is bound to
   an eslint-disable-next-line directive that must stay adjacent to the line
   it suppresses; converting it would put the directive, not the doc comment,
   closest to the declaration.

==============================================================
2. Non-durable plan/round numbering in non-test source
==============================================================
   ADR- and spec-anchored forms ('ADR-0018 decision 4', 'arch §Simulator &
   Time point 2') name the record and are durable by the rule's own
   definition, so they are deliberately retained. .claude/rules/engineering.md
   is excluded because it quotes the banned patterns as its own examples.

   (zero matches)

   Out-of-scope survivors (test and e2e files are excluded from this work
   package's declared scope), recorded for follow-up:
   e2e/pickem-journey.sim.spec.ts:180:// Members are collapsed by default (feedback round 6), so anything asserted as
   e2e/pickem-journey.sim.spec.ts:213:  // tab (round 5). Which URL that tab lands on is routing; what matters to every
   apps/api/test/invites-join.test.ts:502:describe("custom maxMembers cap (feedback item 10)", () => {

==============================================================
3. Comment-only proof
==============================================================
Files changed in the delivery: 60
Method: strip every // and /* */ comment (string- and template-literal aware),
collapse whitespace, compare the base blob against the working file.

RESULT: all 60 files are byte-identical once comments are stripped.
No runtime token changed anywhere in this delivery.
