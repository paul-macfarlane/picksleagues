---
name: worktree
description: Bootstrap a sibling git worktree so a second session can work in parallel (branch, env files, install)
argument-hint: <task-id | branch-name>
---

Read and follow `AGENTS.md`, then `docs/harness/workflows/worktree.md`.
Use **$ARGUMENTS** as the request arguments. The shared workflow is authoritative; do not maintain a second copy here.
