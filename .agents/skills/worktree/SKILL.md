---
name: worktree
description: Bootstrap a sibling git worktree so a second session can work in parallel
  (branch, env files, install)
---

Read and follow `AGENTS.md`, then `docs/harness/workflows/worktree.md` (paths relative to the repository root).

Use the arguments supplied in the user’s request. The shared workflow is authoritative; do not maintain a second copy here.
