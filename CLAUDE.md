# Picks Leagues — Claude adapter

@AGENTS.md

@docs/harness/engineering.md

Shared instructions and workflows live in `AGENTS.md` and `docs/harness/`. Keep this file as an adapter only. Claude-specific permissions and hooks remain in `.claude/settings.json` and `.claude/hooks/`.
