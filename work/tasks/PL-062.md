---
id: PL-062
title: cron-job.org configuration
status: complete
commits: TBD
---

## Summary

All four sync endpoints (`/api/cron/nfl/setup`, `/weekly-sync`, `/odds-sync`, `/live-scores`) and bearer-token auth (`lib/cron-auth.ts`) were shipped in earlier stories. PL-062 had two deliverables: (a) document the exact cron expressions, timezone, method, headers, and retry configuration in `docs/BACKGROUND_JOBS.md` as a single source of truth; (b) configure the four jobs in cron-job.org (user did this out-of-band).

## Business Spec Coverage

N/A — deployment infra.

## Decisions

- **All jobs use America/New_York timezone, not mixed UTC**: Consistency wins. Also aligns with the NFL schedule's native timezone.
- **Initial Setup job is disabled in cron-job.org**: Fires only via "Run now" button when seeding a new season; does not run on a schedule.
- **Fold operational config into BACKGROUND_JOBS.md**: Deviates from original spec ("set up out-of-band only" like PL-060/PL-061), but gives a single source of truth to prevent drift between code, docs, and the cron-job.org dashboard.

## Deviations from the Story

- Added `docs/BACKGROUND_JOBS.md` section with exact cron expressions, method, headers, and retry counts—originally the story suggested no doc deliverable.

## Notable / Gotchas

- **Prettier drift on PL-080 task notes**: Found while running `pnpm check`; committed separately as `chore: prettier drift fix on PL-080 task notes` (sha `4c681e1`) so it didn't pollute the PL-062 commit.

## Follow-ups

- PL-063 (custom domain) still pending.
