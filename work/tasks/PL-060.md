---
id: PL-060
title: Vercel deployment (free tier)
status: complete
commits: N/A — set up out-of-band by user
---

## Summary

Vercel project connected to the repo; environment variables configured. Done by the user outside of a Claude session on 2026-04-12 so CD is live from the start of Foundation.

## Business Spec Coverage

N/A — deployment infra.

## Deviations from the Story

- Pulled forward from Section 7 (Deployment) into Section 1 (Foundation) so we can deploy + test continuously rather than saving deployment for the end.

## Follow-ups

- PL-004 left env vars as `!` non-null assertions in `lib/auth.ts` and `lib/db/index.ts`. A boot-time validation module would fail fast on missing env; worth adding now that prod deploys are live.
- PL-062 (cron-job.org configuration) still pending — cron endpoints don't exist yet (PL-014).
- PL-063 (custom domain) still pending.
