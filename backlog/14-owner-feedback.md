# Epic: Owner Feedback (FB)

Items the owner raised from real use of the app rather than from the spec. None
of them block launch — that is why the epic sits after `09-launch` in the build
order — but each is a thing the owner noticed and wants fixed once the product
is out. Numbering follows the order they were raised, so an item stays traceable
to the round it came from.

Some of these are **questions before they are changes** (FB-2, FB-15, FB-30,
FB-40, FB-44): the deliverable is a written answer with evidence, and code only
if the answer calls for it.

- [x] **FB-1** — _(resolved 2026-08-10: the stored replay fixture predated the logo carry-through commit — re-importing the season heals it (proven on dev, 0/32 → 32/32 logos); no live-sync bug; regression test added in `sim-replay.test.ts`)_ — Team logos are missing on the 2025 replay fixture. Establish which layer drops them before changing anything: the replay capture already carries logos through from `espn.fetchNflTeams()` keyed by provider team id and nulls them only when the listing lacks that team (`apps/api/src/services/sim/replay.ts`), while the hand-authored scenarios synthesize logo URLs from the abbreviation (`services/sim/scenarios/teams.ts`) — so a replay-only symptom points at the listing lookup, and a symptom on both points at the surface that renders them. Fix where the fault actually is; a fixture patched by hand would hide a live-sync bug rather than prove one isn't there. _(deps: none)_
- [x] **FB-2** — _(resolved 2026-08-10: full `settle-sweep` at the stated bound — 8 active league seasons, 144 league-weeks, 6,048 results — completes in ~0.6s locally (~75ms/season); nothing to fix; measurement in `docs/evidence/test-results/FB-2/`)_ — Audit database query performance, settlement first. The bound the audit is written against is **a handful of leagues settling in the same window** (SWP-7 shrank it from "dozens to low hundreds" — the real audience, not internet scale; an audit without a stated bound has no pass mark and will either over-engineer or clear anything). At that bound the audit collapses to **one timing of a full `settle-sweep` against a realistically sized dev dataset** — fix only what that measurement shows, which is plausibly nothing. Deliverable is the measurement plus whatever the measurement justifies. Ref: arch D10; `apps/api/src/services/settlement.ts`. _(deps: none)_
- [x] **FB-3** — _(wontfix — owner scrapped 2026-08-09: complexity above the feature's gain for a friends-scale app)_ Standings movement for Pick'em: show each member's change in rank against the previous week's standings, so the board reads as a race rather than a snapshot. **Derived at read time, never stored** — arch D10 makes standings recomputable from (picks, results, settings), and a persisted delta column is exactly the state a full recompute would not reproduce. The prior week's standings are already reproducible from the same inputs. Ref: spec §Screens. _(deps: none)_
- [x] **FB-4** — _(wontfix — owner declined 2026-08-10: not wanted at the moment)_ Scheduled dependency updates, automated. The engineering rules already require preferring latest stable (§Quality), which today depends on someone remembering; the failure that automation prevents is the one the rule names — deferred majors arriving together on the day a security fix ships only on the newest. Explore the options (Renovate vs Dependabot, grouping, auto-merge on green) and pick one. GitHub Actions minutes are free on this public repo, so cadence is a noise question, not a cost one. _(deps: FND-8)_
- [x] **FB-5** — _(resolved 2026-08-10: overview shows the season board only; selector, `week` search param, and dead client week plumbing removed; ADR-0035 + spec §Screens amended)_ — Drop the week selector from the standings on the league overview and show the latest standings only. Weekly performance is already reachable — League Picks has its own week selector and shows the week in more detail than the overview ever could — so the overview's selector is a second way to ask the same question, on the page least suited to answering it. Removing it simplifies the page, which is the point. Ref: spec §Screens. _(deps: none)_
- [x] **FB-6** — _(resolved 2026-08-10: self-demotion gets the app's AlertDialog confirmation ("Step down as commissioner?"); demoting another commissioner stays one click since the actor can reverse it; server ≥1-commissioner guard was already in place per ADR-0004; the Leave button shares the sole-commissioner disable-with-reason so no surface confirms a leave the server must refuse)_ — Guard a commissioner against accidentally demoting themselves. A commissioner who demotes the last commissioner (or themselves generally) cannot undo it from the UI, which is what makes this worth a confirmation the other member actions don't get. **Check the pattern before adding one:** the app's existing destructive-action treatment is what this should match, so if a modal is not what the app already does elsewhere, propose what it does instead rather than introducing a second idiom. Ref: spec §Membership. _(deps: none)_
- [x] **FB-7** — _(wontfix — owner scrapped 2026-08-09: superseded by the broader complexity-vs-value sweep, which carries the playoff question as one candidate among many)_ Decide whether Pick'em playoff support earns its complexity, or should be cut. This **reopens a settled decision**: ADR-0007 (2026-07-22) put playoffs in scope, and SIMP-17's season-range presets carry it as the Postseason and Full Season options. The question is whether any real league will use it — a playoff Pick'em league is a four-week league with slates of 6/4/2/1 games, and the whole of the support is the week-ordering and short-slate handling that already exists. So the honest framing is not "what does it cost to keep" but "what would we delete": the presets collapse to Regular Season only, matching Survivor. Answer first, with the deletion scoped; an ADR superseding ADR-0007 plus a spec amendment only if the answer is to cut. _(deps: none)_

Round 6 — raised 2026-08-11 from staging testing, after round 5 (PR #81).
FB-42 and FB-43 are the same two surfaces and ship together — adding the score
to a history row is a layout change to the row it lands in, so doing them apart
would mean laying that row out twice. Priority order as before.

- [x] **FB-41** — _(resolved 2026-08-11: `/join/<code>` is served by the API function ahead of the SPA fallback, returning the built shell with four meta tags rewritten to name the league, its mode, and the spots left — the document is otherwise byte-identical, so a human following the link boots the same SPA. Owner chose the league-naming shape over a generic one; ADR-0038 records what the sessionless read discloses. A revoked or unknown code unfurls generically so it can't report whether a code was ever real. Verified against the built artifact, not just the dev route)_ — An invite link unfurls as the generic app preview, so a link pasted into a text thread reads as "Picks Leagues — Season-long sports leagues with friends" rather than as an invitation. Every route falls through to the one static `index.html` (`scripts/build-vercel-output.sh`'s SPA fallback), whose `og:*` tags are fixed at build time, so **no per-route preview is possible without serving `/join/*` its own HTML**. Two shapes, and the choice is the deliverable's scope: a build-time copy of the shell with invite-flavoured tags (no league name, no runtime cost, no DB read), or a function that injects the league's own name per code. Note what the second one is not: a disclosure problem. The unfurling platform and everyone in the thread can already open the link and read the name — invite codes are bare and unauthenticated by design (ADR-0032). Note also what neither can be: verifiable locally. Vite serves `/join/*` itself in dev, so the tags only exist in a Vercel build, and the proof is a staging deploy plus a real unfurl. _(deps: none)_
- [x] **FB-42** — _(resolved 2026-08-11: both surfaces share one row frame — an outcome-coloured left rule, the pick and its verdict on a `text-sm` top line pushed to opposite ends, the game and its score on a muted line beneath. Owner picked the direction. The colour is redundant reinforcement only: strip it and the glyph and word still carry the verdict)_ — Pick'em's League Picks rows and the Survivor overview's pick surfaces are hard to read. Both are dense `text-xs` blocks of muted-on-muted text where the pick, the verdict, the line, and the game state all carry the same weight, so nothing anchors the eye and a member's week has to be read rather than scanned. Owner's taste call on direction — the deliverable is a readability pass over `pickem-week-detail.tsx`'s `PickRow` and `survivor-board.tsx`'s row/history entries, at phone width first. Whatever it becomes, it must not cost the testids the merge-gate journeys bind to, and it must stay a *presentation* change: the two surfaces already agree on vocabulary (`PickOutcomeBadge`, `gameStateLabel`, `pickStandingLabel`), and a redesign that forks either is a regression however it looks. _(deps: none)_
- [x] **FB-43** — _(resolved 2026-08-11: history rows render the same `gameStateLabel` the row-level block does, so a pick reads identically in both of its homes; a withheld pick still names nothing)_ — The Survivor overview's pick history shows week, team, and verdict but not the score, while the row-level "This week" block right above it does — so the same pick reads differently depending on which of its two homes you find it in. The data is already on the wire: FB-25 put the override-resolved game state on every *visible* pick, and `PickHistory` simply doesn't render it. One `gameStateLabel` call, in the row FB-42 is relaying out anyway. A withheld pick keeps showing nothing — naming its game would narrow it to two teams (spec §Pick Visibility). _(deps: FB-42)_
- [x] **FB-44** — _(resolved 2026-08-11: answered — the splash and sign-in are unauthenticated front doors where a header could only be a logo and a Sign in button, and they keep none. `/join/:code` was the oversight: its `beforeLoad` guarantees a signed-in member with a claimed username, which is exactly the case `StaticPage` already gives the real `AppHeader` to, so it now wears one and the card centres in what's left)_ — _(question before it is a change, per FB-2's pattern)_ Sign-in, the splash, and the invite-accept page render without the app's top bar. Two of the three are deliberate and should stay: `/welcome` and `/sign-in` are unauthenticated front doors, and a header for a visitor can only be a logo and a Sign in button — which is precisely the `VisitorHeader` that `StaticPage` already falls back to, and neither page needs it (the splash *is* the brand, and sign-in is a centered card whose card carries the mark). **`/join/:code` is the odd one out and looks like an oversight:** its `beforeLoad` redirects to sign-in and then to claim-username, so by the time the card renders the viewer is always a signed-in member with a claimed username — and they are left on a full-viewport card with no way into the app but the one button on it. `StaticPage` already settles what that page should do. _(deps: none)_

Round 5 — raised 2026-08-11 from staging testing, after round 4 (PR #80). Two
of these are **questions before they are changes** (FB-30, FB-40), per FB-2's
pattern. FB-35/36/37/39 are all the discover page and should ship as one change:
pagination, filtering, and sort ordering are the same query, and splitting them
would mean three passes over the same endpoint. Priority order as before.

- [x] **FB-28** — _(resolved 2026-08-11: `LeagueSummary` carries a server-resolved `currentWeekLabel`; the shared `leagueTimingLine` prints “Starts <date>” pre-start and the week (“Week 5”) once under way, compared against `useAppNow()` — the league header shares it and falls back to “Started <date>”, having no week label to show)_ — The dashboard league card says a league "Starts 9/4/25" when the app's now is already past it (owner saw it at simulated 10/2). `startsAt` is the league's derived first-game instant and the card prints it unconditionally (`_authed/index.tsx`, and the same line in `league-header.tsx`) — so once a season is under way the most-visited screen states a future tense about the past. Compare against `useAppNow()`, never `Date.now()` (the sighting is *from* the simulator, so a browser-clock comparison would be wrong in exactly the environment that surfaced it). Decide what a started league says instead: the past-tense date is accurate but low-value, and the card's real answer is which week the league is in — which the summary DTO doesn't carry yet. _(deps: none)_
- [x] **FB-29** — _(resolved 2026-08-11: both surfaces are count-aware — the season-over card reads “You won / You're the last member standing” for a sole winner, and the board header “one member outlasted the rest”; the co-winner wording survives for the case it was written for)_ — A sole Survivor winner is told they are one of several: the picks screen's season-over card reads "you're one of the members left standing" (`survivor-picks.tsx`) and the board's header reads "everyone still standing shares first place" (`survivor-board.tsx`) — both phrased for the co-winner case unconditionally. The board's own row pill already gets this right (`statusLabel` picks "Winner" vs "Co-winner" off `winnerCount`), so the fix is to give these two surfaces the same count-awareness rather than to invent a rule. A sole winner reading a plural is the app hedging away the one moment the season exists to produce. _(deps: none)_
- [x] **FB-39** — _(resolved 2026-08-11: 10 per page, server-side, cut from the fully filtered set so a page can never come up short; response carries page/pageSize/total/totalPages, a page past the end clamps, and the SQL order gained an id tiebreak so paging is stable under equal `created_at`; new shared `ui/pagination.tsx`, which the audit log's hand-rolled pager now uses too)_ — Discovery is not paginated: `discoverLeagues` returns every matching row (`services/discovery.ts`, ordered `createdAt desc`) and the SPA renders all of them. Page it at 10, server-side — a client-side slice would still ship every row, and the ordering FB-37 asks for has to be applied before the cut or page 1 isn't the top 10. The response envelope already exists to grow into (`DiscoveryResponse`), so it gains the page metadata; the SPA gets a `pagination.tsx` primitive, which the UI kit doesn't have yet. Note the wrinkle: the join-cutoff and full-league filters run *after* the rows come back (`isPreStart` + member count), so paging the SQL alone would yield short pages — the filter has to move into the query or the page has to be assembled from it honestly. _(deps: none)_
- [x] **FB-35** — _(resolved 2026-08-11: discovery entries carry a `pickemSettings` summary (pick type + picks per week), parsed through `LEAGUE_SETTINGS_SCHEMAS` so defaults materialize; a chosen subset, never the stored blob — Survivor is null, having no member-facing setting)_ — A discovery card names the mode but not the settings, so a member joins a Pick'em league without knowing whether it's ATS or straight-up, or how many picks a week it takes — the two settings that decide what the league actually asks of them. Surface both on the card. Survivor has no member-facing configurable setting worth showing yet (owner's call, and the settings schema agrees), so this is Pick'em-shaped: read through `LEAGUE_SETTINGS_SCHEMAS` server-side so defaults materialize, and serialize a per-mode summary rather than leaking the settings blob (the DTO is public — anything added to it is visible to non-members by construction). _(deps: none)_
- [x] **FB-36** — _(resolved 2026-08-11: mode renders as a `StatusPill` on the card, and `?mode=` filters server-side alongside the name search; the SPA's filters live in the URL so a browse view is linkable and survives Back)_ — The league type is a `CardDescription` line, which is the same weight as the metadata under it — the one attribute that decides whether a member wants the league at all reads as a caption. Give it visual prominence (the app's `StatusPill` is the existing idiom for a categorical marker), and add a filter by mode. The filter is server-side alongside FB-39's paging for the same reason: filtering a page client-side filters one page. _(deps: FB-39)_
- [x] **FB-37** — _(resolved 2026-08-11: ordered by remaining space **ascending** — the ask said “descending” but gave filling nearly-full leagues as the reason, and that is the ascending order; cards show “7 of 10 members · 3 spots left” so the ordering key is visible)_ — Default discovery's order to available space descending (`maxMembers − memberCount`), so nearly-full leagues surface first and fill rather than every league sitting half-empty. `createdAt desc` is today's order and stays the tiebreaker — two leagues with the same space need a stable second key or the list reshuffles between pages. `maxMembers` isn't on the discovery DTO yet (the card shows a bare member count), so this needs it anyway, and showing "7 of 10" is what makes the ordering legible rather than mysterious. _(deps: FB-39)_
- [x] **FB-34** — _(resolved 2026-08-11: the board's sort floats the viewer to the top of their own group (alive or eliminated), ahead of the elimination ordering; “Out in <week>” already states the standing the position no longer does)_ — On the Survivor board the signed-in member sorts by status and elimination week like everyone else, so finding yourself in a full league means scanning. Float the viewer to the top of their own group — first among the alive, or first among the eliminated. Note what this trades: the eliminated are ordered by how far they got, and the viewer jumping that queue means their row no longer states its own standing by position. That's the owner's explicit ask and the row already carries "Out in <week>", so the information isn't lost — but it's a deliberate override of the board's ordering rule, not an oversight in it. _(deps: none)_
- [x] **FB-32** — _(resolved 2026-08-11: the instant field re-seeds from the clock while it still holds what it was last seeded with — so it always opens on “now”, and the 15s poll still can't clobber an edit in progress)_ — The simulator's "Set an exact instant" field doesn't track the clock. It seeds once from `state.clock.now` at mount and deliberately never re-seeds (`sim-clock-card.tsx`, so a 15s poll can't clobber an operator mid-edit), which means every clock change after mount leaves it showing a stale instant — the operator opens the control expecting "now" and gets wherever the clock was when the page loaded. Reproduce first and confirm that's the mechanism the owner hit (the alternative is the field showing real time on a fresh load, which the seeding says shouldn't happen). Re-seed on clock change while preserving the anti-clobber property the current comment is defending — the two are compatible, they just weren't asked to coexist. _(deps: none)_
- [x] **FB-31** — _(resolved 2026-08-11: “Jump to a kickoff slot” lists the selected week's own distinct effective kickoffs with game counts and lands the clock on one (owner chose derived over hardcoded ET times, so it holds for Thursday/Saturday/international/holiday slates and follows a kickoff override); `−1 week` added to the step buttons)_ — Simulator clock QoL, two parts. **Kickoff slots:** advancing to each NFL window (Sunday 1:00, Sunday 4:05/4:25, Sunday night, Monday night) means nudging by hours and overshooting. Add slot jumps — but derive them from the week's actual kickoffs rather than hardcoding wall-clock times: a hardcoded 13:00 ET is wrong for an international game, a Thursday/Saturday slate, or a holiday window, and it would name an instant no game occupies. The `sim_clock_anchor` set already anchors to a week's games (`WEEK_START`/`BEFORE_FIRST_KICKOFF`/`AFTER_LAST_GAME`), so this extends an existing mechanism rather than inventing one. **Symmetry:** `CLOCK_STEPS` offers "+1 week" but no "−1 week"; add it. _(deps: none)_
- [x] **FB-38** — _(resolved 2026-08-11: `/me` carries `simClockOffsetMs`, so the shell shows non-admins a “Simulated time” bar whenever the clock is shifted; the scenario name and the Simulator link stay operator-only and `/api/sim/state` keeps its admin gate (ADR-0011/ADR-0014))_ — Non-admins can't see the simulated time in staging/dev, so a tester sees lock states and deadlines derived against an instant the UI never states. `SimClockBanner` gates on `isAdmin && simEnabled` because it reads `GET /api/sim/state`, which is admin-only and unregistered when the simulator is off (ADR-0011/ADR-0014) — that gate is correct and must not be loosened. The non-admin variant needs a different source: `/me` already carries both `simEnabled` and the server's `now` (the app clock's offset is learned from exactly that response), so a member-safe banner can be built from what every page already fetches. What it must not show is the scenario name or the Simulator link — those are operator surface. Open question the implementation has to answer: `/me` says the simulator *exists*, not that the clock is currently *shifted*, and a banner that shows at a zero offset trains people to ignore it. _(deps: none)_
- [x] **FB-30** — _(resolved 2026-08-11: answered with a half-pushback — the sportsbook term for a 0 spread is “PK” (*even money* is a price, not a line), but “PK” collides with the mode's own name, so **Even** is right for this app; `spreadLabel` returns it for both sides, unit-pinned alongside the home-relative sign flip)_ — _(question before it is a change, per FB-2's pattern)_ A 0 spread renders as a bare "0" (`spreadLabel` in `lib/game.ts`). The owner proposes "even" and invited pushback. Answer the terminology question, then apply it in the one place spreads are labelled — both sides of a 0-spread game get the same word, which is the point of it. _(deps: none)_
- [x] **FB-33** — _(resolved 2026-08-11: sign-in, the welcome hero, and both `index.html` meta descriptions now name the category (“Season-long sports leagues with friends”) rather than one mode; the meta line names pick'em and survivor with March Madness “to come”, so a shared link doesn't overclaim)_ — "Sports pick'em with friends." (`sign-in.tsx`) and "Sports pick'em leagues with friends." (`welcome.tsx`, plus both `index.html` meta descriptions) predate Survivor and describe one of three modes as if it were the product. Update all four to cover what ships today and doesn't foreclose March Madness — the welcome page already lists all three modes below the line that contradicts it. The meta descriptions matter as much as the visible copy: they're what a shared link previews as. _(deps: none)_
- [x] **FB-40** — _(resolved 2026-08-11: answered — “Discover” promises a curated feed the page doesn't provide (its own title is “Browse public leagues”), and discovery is the rare path since nearly everyone arrives by invite link. Owner chose: nav item renamed to **Browse**, and the dashboard header now carries “Browse public leagues” beside “Create league” so both ways into a league sit where members start)_ — _(question before it is a change, per FB-2's pattern)_ Is "Discover" the right name, and are Discover / Create League in the right place? Today: both are header nav (`app-header.tsx` inline nav and the mobile drawer), the dashboard's empty state offers both as cards, and the dashboard header carries a Create League button. Owner is genuinely undecided and wants joining and creating to be obviously findable. Deliverable is a written recommendation with the reasoning; code only if the answer is to change something. _(deps: none)_

Round 4 — raised 2026-08-10 from staging testing, after round 3 (PR #79). All
Survivor-board work; the owner confirmed Overview-only (no League Picks tab),
which supersedes the door FB-15 left ajar. Priority order as before.

- [x] **FB-25** — _(resolved 2026-08-10: every revealed pick on the standings response carries its game's override-resolved state; withheld picks keep a null game (naming the game narrows a hidden pick to two teams); board renders settled-or-derived badges everywhere; evaluator confirmed the visibility surface clean both rounds)_ — The board's pick entries carry no score, game status, or pre-settlement verdict — a pick just sits there until the week grades. Extend the standings response so each *visible* pick carries its game's effective state (status, scores, kickoff, both team ids), and render outcome badges from the settled grade or the FB-23 derived one. A withheld pick keeps a null game block: naming the game narrows the pick to two teams, which is exactly what §Pick Visibility withholds. Touches visibility serialization — evaluator-gated. _(deps: none)_
- [x] **FB-26** — _(resolved 2026-08-10: alive members' rows carry a “This week” block — team, live game state, verdict, or “No pick in yet” / “In — hidden until kickoff” — and the response carries currentWeekId; history holds the other weeks)_ — Each member's current-week pick surfaces at the row level (when visible per kickoff), with the game's live state beside it — no expanding required; the history disclosure holds previous weeks only. The standings response gains `currentWeekId` (the one definition, FB-11's precedent) so the board doesn't re-derive it. With this, Overview answers "what did people pick this week" at a glance — the reason no League Picks tab is needed. _(deps: FB-25)_
- [x] **FB-27** — _(resolved 2026-08-10: “Revival possible” pill when a member's pick has provisionally lost AND no alive member has secured survival — `survivorRevivalStillPossible` mirrors settlement's math exactly (unit-pinned, 10 cases); ADR-0028's provisional elimination ends the state server-side)_ — A member whose pick has provisionally lost but who is still alive (nobody has secured a win yet, so the everyone-out revival is still possible) reads as plain "Alive", which looks like a bug. Show a "Revival possible" indicator in exactly that state: alive + unsettled pick + derived outcome incorrect. The server already ends the state at the right moment — ADR-0028's provisional elimination flips them to Out the instant someone's survival is certain — so the pill is accurate by construction and needs no new server rule. _(deps: FB-25)_

Round 3 — raised 2026-08-10 from staging testing, after round 2 (PR #78). Same
convention: IDs record raise order, list order is priority order.

- [x] **FB-23** — _(resolved 2026-08-10: the pick row shows the settled grade or, once its game is terminal, the derived one — `survivorProvisionalOutcome` mirrors settlement's gradePick mapping (unit-pinned) — with a one-line “official grading lands when the week finishes” note; the week-2 sighting was the sim's advance-then-sync two-step, which /rules/simulator already explains)_ — A Survivor pick whose game has gone final shows no result, with the note "This week hasn't been graded yet…" (`survivor-game-row.tsx` `ungradedNote`). The *why* is real product behavior, not a simulator artifact — Survivor settles week-atomically (ADR-0025: missed-pick elimination and revival are whole-week answers) — but the member's experience is a completed pick that looks unacknowledged. The window unlock (ADR-0036) already derives win/tie from the game's terminal state instantly, so the row can show the same **derived, provisional outcome** ("Won — grading finalizes when the week completes") without waiting for settlement or violating arch D10 (display derivation, nothing stored). Separately: the owner saw the note persist after advancing to week 2 in the sim — likely the advance-then-sync two-step (grading runs on `sync-scores`/settle, not on the clock moving); verify that's all it was, and consider whether the sim page should say so. _(deps: none)_
- [x] **FB-21** — _(resolved 2026-08-10: success toasts now 2.5s via shared `lib/toast.ts` (errors keep the 4s default — they carry instructions); toaster moved top-center → top-right, riding the empty gutter beside the content column; verified live at 1280px: toast at the right edge below the header, gone in ~2.7s)_ — Success toasts feel like they block bottom-of-screen actions and last too long. Two parts: **duration** — nothing sets one, so every toast holds sonner's 4s default; shorten success toasts (~2–2.5s) while errors keep longer. **Placement** — the toaster is already `position="top-center"` with a header-aware offset, moved there precisely to stop bottom-bar collisions, and that commit is in staging's history — so first reproduce what the owner actually saw (stale deploy? a surface with its own bottom toast? mobile media-query fallback?) before re-fixing placement; don't churn a setting that may already be right. _(deps: none)_
- [x] **FB-20** — _(resolved 2026-08-10: weeks stay in the dropdown; the not-open card now renders the week's slate read-only via the shared `SlatePreview` (matchups, logos, kickoffs) under a scouting-friendly notice, both modes)_ — Weeks ahead of the pick window: keep them in the dropdown and show the week's **slate read-only** beneath a compact "not open yet" notice, instead of the bare notice card. Recommendation (owner asked): members legitimately scout ahead — Survivor is *built* on planning which teams to burn when, and a Pick'em member wants to see next week's matchups — and the slate is public schedule data, so nothing leaks (pick visibility is per-pick and untouched; the write path refuses regardless). Dropping the weeks from the selector would make the list shrink/grow over time and break linkability. Owner confirmed the direction 2026-08-10. _(deps: none)_
- [x] **FB-22** — _(resolved 2026-08-10: once the league starts the panel reads “Joining is closed for this season — existing links can only be revoked.” and the empty state “No invite links are outstanding.”)_ — The invites panel's empty state says "No invites yet." (`invite-panel.tsx`) even once the league has started and joining is closed — copy that promises a future the season can't deliver. When the join window is over, the empty state (and the disabled creation control's framing) should say joining is closed — e.g. "Joining is closed for this season." — rather than implying invites are still coming. _(deps: none)_
- [x] **FB-24** — _(resolved 2026-08-10: the block treatment now applies at every width — `sm:` inline reversion and ml-auto dropped; verified at 1280px)_ — Apply League Picks' mobile block treatment (FB-12: padded three-line rows on a muted background) at all widths instead of reverting to the inline single-line layout at `sm:` — the owner prefers one consistent formatting over the responsive divergence. Drop the `sm:` overrides in `PICK_ROW_CLASS_NAME` and the row's groups; keep whatever inline arrangement still reads well inside the block at desktop width. _(deps: none)_

Round 2 — raised 2026-08-10 from staging testing. IDs still record raise order;
list order within the round is priority order instead (bug fixes ahead of
UX polish ahead of personal taste), since that's the order they should be
picked up in.

- [x] **FB-16** — _(resolved 2026-08-10: `week_not_open` (409) refused outside the pick window — current week, or next once the member's pick's game is terminal win/tie/cancel; derived from game state, not settlement rows; ADR-0036 + spec §Game Mode 2 amended; integration tests added)_ — Survivor let the owner submit a pick two weeks ahead of the current week. Per spec, a Survivor pick should only be allowed for the current week, or for the next week once the current week's pick has resolved win/tie — never further ahead. Locate and fix the pick-window check (likely absent or too permissive server-side, since the client apparently doesn't stop it either). _(deps: none)_
- [x] **FB-13** — _(resolved 2026-08-10: GET /api/me/deletion-blockers names the sole-commissioner leagues; Danger Zone disables Delete with the reason pre-click (owner chose explain-before-click), and the post-click 409 fallback names the real reason too)_ — A sole commissioner who tries to delete their account gets an opaque "could not delete account" failure with no explanation of why (the server-side sole-commissioner guard from FB-6 is working as intended; the UX around it isn't). Pick one: surface the reason on the failed attempt, or detect sole-commissioner status before the Delete button is even clickable and explain it there. Owner leans toward deciding after seeing both, so propose rather than presuming. _(deps: none)_
- [x] **FB-11** — _(resolved 2026-08-10: admin seasons response carries server-resolved `currentWeekId`; games browser, sim clock Jump, and sim fixtures filters default to it, week 1 only when nothing is synced)_ — Week selectors default to week 1 instead of the current week, which is a footgun when advancing the simulator: `sim-clock-card.tsx` (`selectedSeason?.weeks[0]?.id`) and `sim-fixtures-card.tsx` (`DEFAULT_WEEK_NUMBER = "1"`) both do this, and `admin/games.tsx` has the same pattern. Default each to the season's current week instead, falling back to week 1 only when the season has no current week yet (not yet started). _(deps: none)_
- [x] **FB-17** — _(resolved 2026-08-10: same `week_not_open` gate — next week opens once every game in the member's own submission is terminal; no submission waits for the week to turn; ADR-0036 + spec §Game Mode 1 amended; integration tests added)_ — Formalize the same one-week-ahead limit for Pick'em: only the current week is pickable, or the next week once every pick for the current week has resolved. Today this is only accidentally enforced by how far in advance spreads get loaded (per `project_espn_data_facts` — spreads load close to kickoff), which is an ingestion-schedule side effect, not a rule. Make it an explicit check alongside the existing lock-state validation so it holds even if ingestion timing changes. _(deps: none)_
- [x] **FB-12** — _(resolved 2026-08-10: League Picks rows are padded three-line blocks at phone width — pick / grade / game state — separated by a muted background; sm+ keeps the one-line layout)_ — Clean up the mobile layout of the League Picks score breakdown (`leagues/$leagueId/league-picks.tsx`) — each pick, its score, and the game status need to read as visually separated rows rather than a cluttered block at phone width. Mobile-first per engineering rules; verify at phone width first. _(deps: none)_
- [x] **FB-19** — _(resolved 2026-08-10: non-member join blocks render as a destructive-toned inline alert with role=status; already-member stays quiet since it isn't bad news)_ — When a league is full and a member tries to accept an invite, the refusal is correctly enforced but the "league is full" message doesn't stand out — it reads like any other line of text. Give it visual weight (e.g. an inline alert treatment) so a blocked join is unmistakable at a glance. _(deps: none)_
- [x] **FB-9** — _(resolved 2026-08-10: standings columns sized for label + active-sort arrow — rank w-16, record/points w-18; all three clipped when sorted, not just Rank)_ — The Rank column header gets clipped when standings are sorted by rank — the cell is sized for the digits, not the header label. Widen the Rank column (or stop the header from truncating) in the standings table. _(deps: none)_
- [x] **FB-18** — _(resolved 2026-08-10: changing a Survivor pick shows “replaces <team> when saved” in the action bar plus an Undo that drops the unsaved selection)_ — When changing a Survivor pick, show the member's previously submitted pick for that week alongside the new selection for comparison, and give them a way to undo the in-progress change before submitting. _(deps: none)_
- [x] **FB-8** — _(resolved 2026-08-10: already-member invite page button is “Open league” → that league's home; a successful join already landed there; other block reasons keep the dashboard)_ — Accepting an invite (`join.$code.tsx`) sends the member to the dashboard ("Open dashboard" / "Go to dashboard") once they're already a league member. Route to that league's home (`leagues/$leagueId/index.tsx`) instead — the member just joined or already belongs to the specific league the link was for, so the dashboard is a detour through a list they don't need. _(deps: none)_
- [x] **FB-10** — _(resolved 2026-08-10: /rules/simulator restates docs/simulator-guide.md for testers on the public StaticPage shell; linked “How the simulator works” from the Simulator page header)_ — Add a simulator guide alongside the existing rules guide (`apps/web/src/routes/rules/pickem.tsx`, `rules/survivor.tsx`), sourced from `docs/simulator-guide.md`. Non-technical testers on staging (and the owner) need a page that explains what the simulator is and how to drive it without reading engineering docs — mirror wherever the rules guide is linked from so it's discoverable the same way. _(deps: none)_
- [x] **FB-15** — _(resolved 2026-08-10: answered **no** — the Survivor board already shows every member's weekly pick history (revealed per kickoff), elimination week, and consumed teams, so a League Picks page would restate the board week-by-week with no information it lacks; Pick'em needed the page because its board is a points table. Owner accepted; no code.)_ — _(question before it is a change, per FB-2's pattern)_ Does a "League Picks" section add value for Survivor, the way it does for Pick'em? Owner is genuinely undecided. Deliverable is a written recommendation (with the reasoning, not just a verdict) — code only if the answer is to build it. _(deps: none)_
- [x] **FB-14** — _(resolved 2026-08-10: theme selection moved into the account menu as a Theme radio group; top-bar toggle and theme-toggle.tsx removed; no pushback warranted — it's a set-and-forget preference)_ — Move the light/dark mode toggle (`theme-toggle.tsx`) from the top bar (`app-header.tsx`) into the profile menu — owner feels it belongs there rather than as a persistent top-bar control. Nitpick-level; owner is open to pushback if there's a reason to keep it where it is. _(deps: none)_
